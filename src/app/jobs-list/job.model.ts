export class JobData {
    constructor(private id ?: string, private title ?: string, private applylink ?: string, private jd ?: string,
        private companyname ?: string, private location ?: string, private experience ?: string, private salary ?: string,
        private skills ?: string, private enddate ?: string){}
  }